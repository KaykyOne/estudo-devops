services:
  app:
    build: .
    ports:
      - "8089:8080"
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      DB_NAME: fatecads 
      DB_USERNAME: postgres
      DB_PASSWORD: "123456"
